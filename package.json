{
  "scripts": {
    "docker:up": "docker compose up -d",
    "dev": "npm run docker:up && concurrently \"nodemon backend/server.js\" \"cd frontend && ng serve\"",
    "start": "npm run docker:up && concurrently \"node backend/server.js\" \"cd frontend && ng serve\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-fileupload": "^1.5.1",
    "mongoose": "^8.13.2",
    "nodemon": "^3.1.10",
    "concurrently": "^8.2.2"
  }
}
